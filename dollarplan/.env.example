# Local SQLite (Lightsail: persistent volume path)
DATABASE_URL=file:./data/dollarplan.db

# Required when Plaid is enabled later (encrypt access tokens at rest)
# SESSION_VAULT_KEY=base64-encoded-32-byte-key

# Plaid — leave unset until you enable bank sync
# PLAID_CLIENT_ID=
# PLAID_SECRET=
# PLAID_ENV=sandbox
